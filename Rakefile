# import 'service.rake'
# import 'csv_to_json.rake'
# import 'log_backup.rake'
# import 'file_utils.rake'
# import 'clean_and_clobber.rake'

FileList["*.rake"].each { |f| import f }

desc 'help'
task :default do
  puts 'run `rake -T` command to view available tasks'
end
